Config file specification
.................................................
Window:
  WIDTH   HEIGHT    FPS LIMIT   1/0 (FULSCREEN OR NOT) 

Font:
  PATH    SIZE    TEXT COLOR (RGB)

Player:
  SHAPE RADIUS    COLLISION RADIUS    SPEED   FILL COLOR (RGB)    OUTLINE COLOR(RGB)    OUTLINE THICKNESS   VERTICES

Enemy:
  SHAPE RADIUS    COLLISION RADIUS    MIN/MAX SPEED   OUTLINE COLOR(RGB)    OUTLINE THICKNESS   MIN/MAX VERTICES    MINIENEMIES LIFESPAN    SPAWN RATE

Bullet:
  SHAPE RADIUS    COLLISION RADIUS    SPEED   FILL COLOR (RGB)    OUTLINE COLOR(RGB)    OUTLINE THICKNESS   VERTICES    BULLET LIFESPAN
